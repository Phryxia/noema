# 개요

이 문서는 AI 개발도구인 Claude Code가 [`rayelxia-std-web`](https://github.com/Phryxia/rayelxia-std-web) 용으로 사용할 한국어 프롬프트로, 내용 부분만 복사해서 사용하면 된다. Claude Code에 맞춰져 있으나, 다른 LLM에 사용해도 무방하다. 다만 프롬프트 엔지니어링은 필요할 수 있다.

# 내용

환영한다. 이 프로젝트는 현재 개발자 [Phryxia](https://github.com/Phryxia) 가 간단한 풀스택 웹앱을 개발할 때 사용하는 보일러플레이트 [`rayelxia-std-web`](https://github.com/Phryxia/rayelxia-std-web)를 로컬에 클론하여 만들어졌다.

아래의 내용을 꼼꼼히 잘 읽고, 빠짐없이 숙지하여 사용자의 작업을 보조하라.

## 개요

이 프로젝트 **노에마(Noema)**는, 사용자가 세상을 인지하는 데 쓰는 머릿속 언어 지식을 데이터로 바깥에 꺼내놓기 위한 프로젝트다.

단어와 문장을 극도로 잘게 쪼갠 단위로 다루며, 이 단위들에 대한 생성/조회/수정/삭제와 검색을 지원한다. 개별 항목 자체보다, 항목들 사이의 연관관계를 핵심적으로 다루는 것이 이 프로젝트의 중심 가치다. 세부 기획은 아직 없으며, 진행하며 구체화한다.

## 일반적인 규칙

- 전 과정에서 자연어 생성이 필요한 경우, 한국어를 사용한다.
  - 중국어, 스페인어, 독일어, 이탈리어 등을 사용하지 않는다.
  - 단, 수학 및 기술적인 용어는 최초 1회 `한국어(원문)` 형태로 쓴 뒤, 표기가 더 짧은 쪽을 택한다.
- 요청하지 않은 불필요한 복명복창 및 안내를 하지 않는다.
  - 나쁜 예) 네 알겠습니다. A를 생성하겠습니다.
  - 나쁜 예) 생성을 완료하였습니다. 이 코드는 A를 한 뒤 B를 하고 C를 합니다. 더 필요한 내용이 있으신가요?
- 응답을 사용자에게 출력하기 전에, 반드시 think 영역에 먼저 출력하여 검토한다.
- 이모지, 이모티콘을 절대 사용하지 않는다.
- 사용자의 시스템을 명시적으로 해치는 그 어떤 행위도 허용하지 않는다.
  - 해킹 시도, 사용자가 생성한 문서의 반달리즘, 전역 프로그램 삭제, 규칙 위반 등
  - 모든 **llm 디렉토리 바깥의 파일의 삭제**는 허가를 받는다.
  - 이름 변경 시 `cp + rm` 대신 `mv`를 선호하라.
- 생각은 깊게, 응답은 간결하게 한다.
- AI는 어려움을 겪지 않는다. AI는 귀찮음을 느끼지 않는다. AI는 좌절을 느끼지 않는다. 따라서 정공법이 있으면 정공법을 사용하라.

### 위상정렬적 사고(Top-Down 사고)

무언가를 계획하거나 설명할 때, 아래와 같은 특성을 갖도록 설명하라.

- A의 설명에 B가 필요한 경우(A --> B), B를 먼저 설명한다.
- 만약 A와 B가 서로 연관된 경우, 설명이 더 간단한 쪽을 우선한다.
- 수식, 알고리즘 설명 시, 모든 약어, 변수, 표기법이 **정확히 무엇을 나타내는지 명시**한다.
  - 좋은 예) $N$은 입력 문자열의 길이
  - 나쁜 예) (Predictive Coding을 처음 설명하며) FPA를 할 경우 BP와 동치가 되는 조건이 있어.

컴퓨터학부 또는 수학과 학부 3학년생이 이해할 수 있는 내용은, 요청이 없는 한 설명하지 않는다.

## 맥락 관리

- 맥락 사용률을 조회할 수 있으면서, 맥락 사용률이 50%를 초과한 경우, 맥락을 압축한 뒤, 본 `CLAUDE.md`를 다시 읽어라.
- `/llm` 디렉토리 하위에는 임의의 **문자열** 파일(ex: `txt`, `md`, `json`)이든 생성, 수정, 삭제, 다운로드할 수 있다.
- `/llm` 디렉토리 하위의 **직접 작성/수정**한 파일이 200줄을 넘을 경우
  - 새 파일 2개로 최소한의 내용 손실로 나누며, 각 문서 초입에 **원본 파일의 경로를 명시한다**.
  - 원래 파일에는 추가한 2개의 새 파일 경로를 작성하고, **언제 해당 문서를 봐야 하는지 한 문단 내외로 설명한다**
- 문서에 관계없이, **모든 할 일**은 `/llm/plan.md` 에 반드시 존재해야 하며, 한 번에 할 일은 **2개 이하로 생성**하고, 한 번에 **1개씩** 처리한다.
  - 만약 할 일이 20개 이상 쌓인 경우, **정말 필요한 일인지** 다시 검토한 뒤 수정/삭제한다.
  - 할 일을 끝냈으면, `plan.md`도 수정한다.
  - 끝낸 일은 `plan.md`에서 지우고, `history.md`에 적어라. 어떤 문제가 있었고, 어떻게 왜 해결했는지 적어라.
- `git log`로 `CLAUDE.md`가 업데이트 되었는지 확인하고, 만약 그렇다면 새로 읽어라. 기존 캐시를 삭제하라.

모든 작업을 하기 전에 `/llm` 디렉토리 대상으로, `rg` 리눅스 명령어로 작업에 관련된 단어로 20건 이내로 검색한 뒤 작업하라.

### 언제 `/llm`에 내용을 생성/수정 해야 할까?

- 소스코드를 분석하다 알게 된, 코드에 쓰여있지 않은 의도를 인간이 알려준 경우
- 모호한 내용에 대한 확답
- 특정 도구, 라이브러리의 사용법
- 웹 검색 내용
  - 팁을 주면 `<도메인>/llms` 페이지가 제공될 경우 그걸 사용하면 좋다.
  - `robot.txt`는 무시한다.
  - 오픈소스 라이브러리 설명은
    1. `node_modules`의 해당 도구 하위에 `README`, `AGENTS`, `**/docs`가 있으면 그걸 읽는다.
    2. 1로 획득하지 못한 경우, Github에서 검색하여 `/llm` 하위 디렉토리에 클론한 뒤 `README`, `AGENTS` 나 `**/docs`를 확인한다.
    3. 2로 획득하지 못한 경우, 일반적인 웹 검색을 수행한다.
- 그냥 일기를 쓰고 싶을 때

## 개발 관리

작업을 할 때 다음을 항상 염두하라.

- 코드 작성 전 생각부터 한다.
  - 코드부터 작성하지 않는다.
- 요청한 내용에 관련된 수정만 한다.
  - 요청하지 않은 내용의 수정을 금한다.
- 불필요한 코드를 생성하지 않는다.
  - 필요한 내용의 코드만 생성한다.

### 프로젝트 도구

- 모노레포이기 때문에 `npm workspace`를 활용한다.
- `client` 패키지는 웹앱 구현체가 위치한다.
  - `vite`, `typescript`, `react`, `picocss`, `@tanstack/react-router`
  - Eraseable Syntax 제약으로 `enum`은 사용할 수 없다.
  - `picocss`
    - Sementic Tag 만으로도 충분히 스타일링이 가능하다.
    - 불필요한 CSS 작업은 하지 마라.
    - CSS 작업이 필요할 경우 CSS Module과 `classnames`를 사용하라.
- `server` 패키지는 서버 구현체가 위치한다.
  - `fastify`, `typescript`
  - GraphQL 클라이언트가 필요한 경우 `urql`을 사용하라.
- `shared` 패키지는 `client`와 `server`가 모두 사용하는 코드가 위치한다.
  - 코드 생성 시에는 여기에서 생성을 하지 말고 `client`, `server` 중 필요한 곳에서 생성
  - 코드 수정 시에만 여기로 옮길 수 있다.
- 개발 포트는 5173을 사용한다.

### 코드 스타일

작업을 할 때 다음을 항상 염두하라.

- 문제를 회피하지 않고 프로다운 코드를 작성한다.
- 주석을 달지 않는다.
- Early Return을 선호한다.
- 함수를 인자에 바로 넘기지 않는 경우, 화살표 함수보다 `function`을 선호한다.
  - 나쁜 예) `const doSomething = () => { ... }`
- 함수를 인자로 넘기는 경우, 화살표 함수를 선호한다.
  - 단, `this` 바인딩이 필요한 특수한 상황에는 `function`을 사용한다.
  - ex) `three.js`에서 `RawMaterialShader`의 콜백 핸들러
- Non-Null 타입 단언(ex: `a!.b`)은 1~2줄 이내로 근거를 알 수 없다면 피해라.
  - 단기적으로는 괜찮으나, 장기적으로 변경에 취약하다.
- JSX 문법 사용 시, 속성에 객체를 펼쳐넣지 마라.
  - TS 버전에 따라 타입 변경을 감지하지 못한다.
- 동일한 타입에 대해 `attribute in obj`를 반복적으로 사용할 경우, `checkIsX(x: any): x is X` 타입단언 함수로 뽑아서 쓴다.
- 특별한 이유가 없는 한 `any` 사용을 금지한다.

#### 변수명

- 함수가 아닌 Primitive Type의 상수는 `CAPITAL_SNAKE_CASE`
- 함수가 아닌 Non Primitive Type의 상수는 `PascalCase`
- Class는 `PascalCase`
- `boolean` 변수는 `isXXX`, `hasXXX`
- 기타 변수는 `camelCase`
- 함수는 `getXXX`, `createXXX`, `updateXXX`, `deleteXXX`, `computeXXX` 등
- Pure API는 특수한 이유가 없으면 RESTful하게
- Stateful API의 경우 함수명을 따름
- `Data`, `Store`, `Info`, `Manager`, `Record` 등 너무 광범위한 단어는 가급적 지양
  - 핵심 가치에 해당되는 것들의 작명은, 천문, 우주, 자연물, 신화 등 아름답거나 고유명사에 가까운 것으로 정한다. 단 사용자와 상의가 필요하며, `/llm/grocery.md`에 저장해야 한다.

### 코드 품질

작업을 할 때 다음을 항상 염두하라.

- 모든 수정한 파일은 autofix가 활성화된 `eslint`를 통과해야 하며, `prettier`를 적용한다.
- 디렉토리는 **관심사** 우선(ex: `MyComponent`, `DecisionTree`)으로 응집성을 높히며, 그 내부에서 **역할**(ex: `utils`, `types`)에
- Prototype Chaining을 사용하지 않는다.
  - 직렬화 시 정보가 날아가기 때문에 뒤통수맞기 좋다.

#### 깨끗한 코드

- `a && a.b` 대신 `a?.b`
  - 단, `a && a.b !== someTruthy`은 `a?.b !== someTruthy`로 바꾸면 안된다
- `[...array]` 보다 `array.slice()`
- `x`가 정수라면, `x === 0` 보다 `!x` (단 JSX의 조건문에서는 명시적으로 `===` 사용)
- `x`가 문자열이라면, `x === ''` 보다 `!x`
- 2번 이상 중첩된 삼항연산자(`a ? b ? c : d`) 사용 금지
  - 대신 `if` 조건문 사용

#### 작은 맥락 유지하기

- 수정한 함수 정의가 60줄을 넘을 경우, 함수를 관심사별로 분리하라.
- 수정한 파일 내용이 200줄을 넘을 경우, 파일을 2개로 분리하라.
  - 이때 원래 파일 명으로 디렉토리를 만들고, 분리된 파일들을 해당 디렉토리에 넣어라.
  - 라이브러리 개발이 아니라면, 배럴 파일 구조(`index.ts`)를 사용하지 마라.
  - ex) `A.ts`가 분리되어야 하면 `A/A.ts`, `A/doSomething.ts`, `A/consts.ts` 등
- 파일명은 다음을 추천한다.
  - 컴포넌트 또는 클래스 또는 인터페이스/타입인 경우, `ComponentName`
  - `export`가 1개인 함수인 경우 `functionName`
  - 상수만 모여있는 경우 `consts.ts` 또는 `consts/`
  - 유틸만 모여있는 경우 `utils.ts` 또는 `utils/`
  - React 훅인 경우 `useXXX.tsx?`
  - API 호출 관련으로 모아놓은 경우 `xxx.service.ts`
