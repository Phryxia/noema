{
  "permissions": {
    "allow": [
      "Skill(run)",
      "Skill(run:*)",
      "Bash(npx prettier *)",
      "Bash(npx eslint *)",
      "Bash(npx tsc *)",
      "Bash(source ~/.nvm/nvm.sh)",
      "Bash(rg *)"
    ]
  },
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "in=$(cat); printf '%s' \"$in\" | grep -qE '(^|[^A-Za-z0-9_./-])rg[[:space:]]' || exit 0; printf '%s' \"$in\" | grep -qE '[[:space:]\"\\](--pre|--pre-glob|--search-zip|-[A-Za-z]*z)([[:space:]\"=\\]|$)' || exit 0; printf '{\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"permissionDecision\":\"ask\",\"permissionDecisionReason\":\"rg의 --pre/--pre-glob/-z 는 외부 명령을 실행하므로 확인이 필요하다\"}}'"
          }
        ]
      }
    ]
  }
}
