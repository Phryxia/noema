# 도구

`/tools`. 기존 단어를 조합하거나 복잡한 관계를 한 번에 짓는 기능을 모은 페이지. 공통 헤더의 `도구` 링크로 들어간다. 상단 탭 서브 네비게이터(`ChoiceGroup`)로 도구 하나를 골라 노출하며, 선택은 `?tab=` 쿼리에 실린다.

## 코드

- 디렉토리: `packages/client/src/tools/`
- 페이지: `routes/tools.tsx`(`validateSearch`) -> `tools/ToolsPage.tsx`, 공용: `tools/keepFieldFocus.ts`
- 순서화: `tools/ordering/OrderingTool.tsx`, `useOrderingTool.ts`, `submitOrdering.ts`, `computeAdjacentPairs.ts`(+test), `consts.ts`(`NEXT_RELATION_NAME`, `MIN_ORDERING_WORD_COUNT`)
- 다국어 매핑: `tools/i18n/I18nMappingTool.tsx`, `useI18nMappingTool.ts`, `submitI18nMapping.ts`, `computeMappingTuples.ts`(+test), `checkMappingReady.ts`(+test), `MappingResultDialog.tsx`, 가변행렬 `tools/i18n/MatrixField/`(`MatrixField.tsx`, `computeMatrixNavigation.ts`(+test), `resizeMatrix.ts`(+test))
- 공용 배치 절차와 결과 표: [관계-배치](./관계-배치.md)

## 사양

### 서브 네비게이터

- 탭은 순서화(`order`)와 다국어 매핑(`i18n`) 둘. 선택된 탭의 도구만 렌더한다.
- `?tab=i18n`만 유효한 쿼리다. 값이 없거나 무효하면 순서화로 동작하고 URL에 쿼리를 되쓰지 않는다. 순서화 선택은 키 제거(`tab: undefined`)다.
- `validateSearch`가 무효 키를 떨어뜨리지만 직접 진입 시 `useSearch`가 원본을 줄 수 있어 `ToolsPage`가 `tab === 'i18n'`으로 재검증한다.

### 순서화

- 단어 리스트 `[w1, ..., wn]`을 받아 인접한 두 단어마다 관계 짓기 `(w_i, w_{i+1}, 다음)`을 만든다. `w_i`가 `word1`, `w_{i+1}`이 `word2`, 관계 이름 `word3`은 고정 단어 `다음`.
- 입력칸은 알려주기 예문 만들기의 동적 칸(`SubjectWordFields`, `isCountAdjustable`)과 같다. 처음 2칸, 세 번째부터 `(optional)`, 주사위 없음.
- 채워진 단어가 2개 이상이어야 제출 가능. 빈 칸은 위치와 무관하게 무시하고 채워진 단어끼리 순서대로 쌍을 만든다. 같은 단어가 겹쳐도 막지 않는다.
- 제출 버튼은 입력칸 바로 아래. 절차·결과 표·토스트는 배치 모드와 같다. 성공이 하나라도 있으면 첫 칸에 포커스한 뒤 칸을 2칸으로 되돌린다.

### 다국어 매핑

- 가변행렬 표(최소 1x1, 초기 2열x3행). 각 열이 같은 언어, 각 행이 같은 의미의 단어다. 첫 행은 언어 이름 행으로 placeholder `언어1`, `언어2`, ...를 갖는다.
- 표 오른쪽 10px에 표 높이만큼의 `+`(열 추가), 아래 10px에 표 폭만큼의 `+`(행 추가), 위 10px에 열마다 `-`(열 삭제), 왼쪽 10px에 행마다 `-`(행 삭제). 크기 1이면 삭제 버튼 비활성.
- 커서 이동: 끝 커서 오른쪽 키는 다음 열 처음(마지막 열이면 다음 행 첫 열), 시작 커서 왼쪽 키는 이전 열 마지막(첫 열이면 이전 행 마지막 열), 위/아래 키는 이웃 행 같은 열의 같은 문자수(부족하면 끝). Tab은 캐럿과 무관하게 다음 칸 처음. Enter는 아래 이동이되 마지막 행에서는 삼킨다(암묵 제출 방지). 그 외 이동할 칸이 없으면 브라우저 기본 동작. 탭 문자는 붙여넣기를 포함해 입력되지 않는다.
- 제출 버튼은 개별 문서 페이지처럼 bottom sticky. 데이터가 있는 모든 열의 언어 이름이 채워지고 만들 관계가 1개 이상이어야 활성.
- 제출은 행렬의 모든 비어있지 않은 단어(헤더 포함)를 `upsertWord`로 확보한 뒤, 데이터 행마다 채워진 두 열 `(j, k)`에 대해 `(matrix[i][j], matrix[i][k], matrix[0][k])` 관계 짓기를 만든다. 언어 이름이 빈 열로 향하는 관계는 제외. 같은 값 삼중쌍은 한 번만 시도하고, DB 중복은 건너뛴다.
- 완료 시 모달(`MappingResultDialog`)에 신규 단어를 콤마로 나열하고 추가된 관계 수(중복 제외)를 표기한다. 0개여도 모달을 띄우며 배치 모드의 전부-중복 throw 경로를 타지 않는다. 닫으면 행렬이 초기 크기로 리셋(리셋 자체는 제출 직후)되고 첫 칸에 포커스한다.

## 결정 근거와 제약

- 결과를 표+토스트로 보이는 도구는 `submitNamedAssociations`·`useBatchSubmission`·`BatchResultTable`을 재사용한다(순서화). 결과를 모달로 보이는 도구는 `submitNamedAssociation`(단일 튜플)과 `useMutation`을 직접 쓴다(다국어 매핑). 도구 고유 로직(쌍·튜플 만들기, 커서 판정)은 순수 함수로 두어 테스트한다.
- 다국어 매핑의 세부 판단(튜플 사전 dedup, Enter 삼킴, 리셋/포커스 분리 등)은 [일지](../일지/2026-08-31-도구-다국어-매핑과-탭-네비게이터.md)에 있다.
- 초기화 순서가 포커스 먼저, 상태 리셋 나중인 이유는 [입력-포커스참고](../참고/입력-포커스참고.md#상태-갱신과-포커스-순서).

## 알려진 함정

- 버튼 mousedown의 포커스 탈취(`keepFieldFocus`): [입력-포커스참고](../참고/입력-포커스참고.md#클릭-소실-mousedown과-blur)
- strict `useSearch`도 직접 진입 시 원본 search를 줄 수 있다: [라우팅-쿼리참고](../참고/라우팅-쿼리참고.md#tanstack-router)

## 관련 문서

[관계-배치](./관계-배치.md), [관계](./관계.md)

## 일지

- [2026-08-31 도구 다국어 매핑과 탭 네비게이터](../일지/2026-08-31-도구-다국어-매핑과-탭-네비게이터.md)
- [2026-08-15 도구 페이지와 순서화](../일지/2026-08-15-도구-페이지와-순서화.md)
