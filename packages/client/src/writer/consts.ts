import type { TextWriterMode, WriterMode } from './types'

export const WriterModes: WriterMode[] = ['단어', '문장', '문서']

export const TextWriterModes: TextWriterMode[] = ['단어', '문장']

export const SentenceOnlyModes: TextWriterMode[] = ['문장']
