{
  "name": "noema",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev:client": "npm --workspace=client run dev",
    "dev:server": "npm --workspace=server run dev",
    "preview": "npm --workspace=client run preview",
    "build": "npm --workspace=client run build && npm --workspace=server run build",
    "build-gh-page": "npm --workspace=client run build -- --base=/noema/ --outDir ../../docs --emptyOutDir",
    "test": "npm --workspace=client run test && npm --workspace=server run test",
    "generate-db": "npm --workspace=server run generate-db",
    "analyze": "npx ts_dependency_graph --hotspots --color_edges=false --agg --start ./packages/client/src/routes/**/* | dot -Tsvg -Gsplines=ortho > graph.svg",
    "analyze-llm": "npx ts_dependency_graph --start ./packages/client/src/main.tsx"
  },
  "license": "GPL-3.0-only",
  "workspaces": [
    "packages/client",
    "packages/server"
  ],
  "devDependencies": {
    "@eslint/js": "^9.36.0",
    "eslint": "^9.37.0",
    "eslint-plugin-import": "^2.32.0",
    "playwright": "^1.61.1",
    "prettier": "^3.6.2",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.46.3",
    "utility-types": "^3.11.0",
    "vitest": "^4.1.10"
  },
  "dependencies": {
    "dotenv": "^17.2.3"
  }
}
